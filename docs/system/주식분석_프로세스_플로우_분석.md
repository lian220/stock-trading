# 주식 분석 프로세스 플로우 분석 및 개선 계획

## 📋 현재 상태 분석

### 1. API 엔드포인트 동작 방식

#### `/economic/update` (경제 데이터 수집)
- **날짜 파라미터 없음**: 오늘 날짜 기준으로 수집
  - `get_last_updated_date()`로 마지막 수집 날짜 조회
  - 마지막 날짜+1부터 오늘까지 수집
  - `storage_end_date = today` (오늘 데이터도 저장)
- **날짜 파라미터 있음**: 지정된 날짜 범위로 수집
  - `start_date` ~ `end_date` 범위 수집 및 저장

#### `/recommended-stocks/generate-technical-recommendations` (기술적 지표 분석)
- **날짜 파라미터 없음**: 오늘 날짜 기준으로 분석
  - 최근 6개월 데이터로 지표 계산
  - 결과는 오늘 날짜로 저장
- **날짜 파라미터 있음**: 지정된 날짜 범위로 분석
  - 해당 날짜 범위의 데이터로 지표 계산
  - 결과는 `end_date`로 저장

#### `/recommended-stocks/analyze-news-sentiment` (감정 분석)
- **날짜 파라미터 없음**: 오늘 날짜 기준으로 분석
  - Alpha Vantage API로 최근 3일 뉴스 수집
  - 결과는 오늘 날짜로 저장
- **날짜 파라미터 있음**: 지정된 날짜 범위로 분석
  - `start_date` 기준으로 뉴스 수집
  - 결과는 `end_date`로 저장

### 2. 스케줄러 동작 방식

#### 스케줄러 실행 시간표
```
06:05 - 경제 데이터 수집 (오늘 데이터 포함)
23:00 - 경제 데이터 재수집 (오늘 데이터 포함)
23:15 - 병렬 분석 (Vertex AI + 기술적 지표 + 감정 분석)
23:45 - 통합 분석 (기술적 지표 + 감정 분석)
00:00 - 자동 매수
```

#### `_run_economic_data_update()` (경제 데이터 수집)
- **현재**: 날짜 파라미터 없이 호출
  - `update_economic_data_in_background()` → `fetch_economic_data()`
  - 자동으로 마지막 날짜부터 오늘까지 수집
- **문제점**: 스케줄러는 특정 날짜 기준으로 동작해야 하는데, 항상 "오늘" 기준으로 동작

#### `_run_parallel_analysis()` (병렬 분석)
- **현재**: 날짜 파라미터 없이 호출
  - `generate_technical_recommendations()` → 최근 6개월 데이터로 분석, 오늘 날짜로 저장
  - `fetch_and_store_sentiment_independent()` → 오늘 날짜 기준으로 분석
- **문제점**: 스케줄러는 어제 데이터 기준으로 분석해야 할 수도 있음 (장 마감 후 다음날 분석)

#### `_run_combined_analysis()` (통합 분석)
- **현재**: 날짜 파라미터 없이 호출
  - `generate_technical_recommendations()` → 최근 6개월 데이터로 분석, 오늘 날짜로 저장
  - `fetch_and_store_sentiment_for_recommendations()` → 오늘 날짜 기준으로 분석
- **문제점**: 스케줄러는 어제 데이터 기준으로 분석해야 할 수도 있음

### 3. 문제점 정리

#### 🔴 주요 문제점

1. **API와 스케줄러의 날짜 처리 불일치**
   - API: 명시적으로 오늘 날짜를 전달
   - 스케줄러: 날짜 파라미터 없이 호출 → 내부 로직에 의존
   - 결과: 스케줄러가 항상 "오늘" 기준으로 동작하지 않을 수 있음

2. **스케줄러의 날짜 기준 불명확**
   - 스케줄러는 특정 날짜 기준으로 데이터를 처리해야 함
   - 예: 23:15에 실행되면 어제 장 마감 데이터 기준으로 분석해야 할 수도 있음
   - 현재는 항상 "오늘" 기준으로 동작

3. **데이터 수집과 분석의 날짜 불일치 가능성**
   - 경제 데이터 수집: 마지막 날짜부터 오늘까지
   - 기술적 지표 분석: 최근 6개월 데이터로 계산하지만, 저장은 오늘 날짜
   - 감정 분석: 오늘 날짜 기준으로 뉴스 수집 및 저장
   - 문제: 분석 대상 날짜와 저장 날짜가 일치하지 않을 수 있음

4. **`generate_technical_recommendations`의 날짜 처리 복잡성**
   - 날짜 파라미터 없음: 최근 6개월 데이터로 계산 → 오늘 날짜로 저장
   - 날짜 파라미터 있음: 해당 날짜 범위 데이터로 계산 → `end_date`로 저장
   - 문제: 분석 기간과 저장 날짜의 관계가 명확하지 않음

## 🎯 개선 계획

### 1. API 동작 원칙 (유지)

**API는 항상 "현재 날짜 기준"으로 동작**
- 날짜 파라미터 없음: 오늘 날짜 기준으로 수집/분석/저장
- 날짜 파라미터 있음: 지정된 날짜 범위로 수집/분석/저장
- **이 원칙은 유지** (사용자가 명시적으로 날짜를 지정할 수 있음)

### 2. 스케줄러 동작 원칙 (개선 필요)

**스케줄러는 "실행 시점의 기준 날짜"로 동작해야 함**

#### 기준 날짜 결정 로직
```python
def get_analysis_date():
    """
    분석 기준 날짜 결정
    - 한국 시간 기준으로 현재 날짜 확인
    - 장 마감 후(16:00 ET 이후)이면 어제 날짜 기준
    - 장 개장 전이면 어제 날짜 기준
    - 장 중이면 오늘 날짜 기준 (실시간 분석)
    """
    korea_tz = pytz.timezone('Asia/Seoul')
    now_korea = datetime.now(korea_tz)
    today = now_korea.strftime('%Y-%m-%d')
    
    # 미국 장 마감 여부 확인
    ny_tz = pytz.timezone('America/New_York')
    now_ny = datetime.now(ny_tz)
    ny_hour = now_ny.hour
    ny_weekday = now_ny.weekday()
    
    # 평일이고 장 마감 후(16:00 ET 이후)이면 어제 날짜
    if 0 <= ny_weekday <= 4 and ny_hour >= 16:
        # 어제 날짜 반환
        yesterday = (now_korea - timedelta(days=1)).strftime('%Y-%m-%d')
        return yesterday
    else:
        # 오늘 날짜 반환
        return today
```

#### 스케줄러 함수 수정 계획

1. **`_run_economic_data_update()`**
   ```python
   def _run_economic_data_update(self, send_slack_notification: bool = True):
       # 기준 날짜 결정
       analysis_date = get_analysis_date()
       
       # 해당 날짜 기준으로 데이터 수집
       asyncio.run(update_economic_data_in_background(
           start_date=analysis_date,
           end_date=analysis_date
       ))
   ```

2. **`_run_parallel_analysis()`**
   ```python
   def _run_parallel_analysis(self, send_slack_notification: bool = True):
       # 기준 날짜 결정
       analysis_date = get_analysis_date()
       
       # 해당 날짜 기준으로 분석
       tech_future = executor.submit(
           self.recommendation_service.generate_technical_recommendations,
           start_date=None,  # 최근 6개월 데이터 사용
           end_date=analysis_date  # 분석 결과는 이 날짜로 저장
       )
       
       sentiment_future = executor.submit(
           self.recommendation_service.fetch_and_store_sentiment_for_recommendations,
           start_date=analysis_date,
           end_date=analysis_date
       )
   ```

3. **`_run_combined_analysis()`**
   ```python
   def _run_combined_analysis(self, send_slack_notification: bool = True):
       # 기준 날짜 결정
       analysis_date = get_analysis_date()
       
       # 해당 날짜 기준으로 분석
       tech_result = self.recommendation_service.generate_technical_recommendations(
           start_date=None,  # 최근 6개월 데이터 사용
           end_date=analysis_date  # 분석 결과는 이 날짜로 저장
       )
       
       sentiment_result = self.recommendation_service.fetch_and_store_sentiment_for_recommendations(
           start_date=analysis_date,
           end_date=analysis_date
       )
   ```

### 3. 서비스 함수 개선 계획

#### `generate_technical_recommendations()` 개선
```python
def generate_technical_recommendations(
    self, 
    send_slack_notification: bool = False, 
    start_date: str = None, 
    end_date: str = None
):
    """
    Args:
        start_date: 분석 시작 날짜 (None이면 최근 6개월)
        end_date: 분석 종료 날짜이자 저장 날짜 (None이면 오늘)
    
    동작:
        - start_date가 None이면: 최근 6개월 데이터로 지표 계산
        - end_date가 None이면: 오늘 날짜로 저장
        - end_date가 있으면: 해당 날짜로 저장
    """
    # 날짜 범위 결정
    if end_date:
        analysis_date = end_date
        if start_date:
            data_start_date = start_date
        else:
            # 최근 6개월 데이터 사용
            end_date_dt = datetime.strptime(end_date, '%Y-%m-%d')
            data_start_date = (end_date_dt - timedelta(days=self.lookback_days)).strftime('%Y-%m-%d')
    else:
        # 오늘 날짜 기준
        analysis_date = datetime.now().strftime('%Y-%m-%d')
        data_start_date = (datetime.now() - timedelta(days=self.lookback_days)).strftime('%Y-%m-%d')
    
    # data_start_date부터 analysis_date까지 데이터로 지표 계산
    # 결과는 analysis_date로 저장
```

### 4. 구현 단계

#### Phase 1: 기준 날짜 결정 함수 추가
- [ ] `get_analysis_date()` 함수 구현
- [ ] 미국 장 마감 시간 확인 로직 추가

#### Phase 2: 스케줄러 함수 수정
- [ ] `_run_economic_data_update()` 수정
- [ ] `_run_parallel_analysis()` 수정
- [ ] `_run_combined_analysis()` 수정

#### Phase 3: 서비스 함수 개선
- [ ] `generate_technical_recommendations()` 개선
- [ ] `fetch_and_store_sentiment_for_recommendations()` 검증
- [ ] 날짜 처리 로직 일관성 확인

#### Phase 4: 테스트 및 검증
- [ ] API 동작 테스트 (날짜 파라미터 없음/있음)
- [ ] 스케줄러 동작 테스트 (다양한 시간대)
- [ ] 데이터 일관성 검증

## 📊 예상 동작 시나리오

### 시나리오 1: API 호출 (날짜 없음)
```
사용자: POST /economic/update
→ 오늘 날짜 기준으로 수집
→ 마지막 수집 날짜+1부터 오늘까지 수집
→ 오늘 날짜로 저장
```

### 시나리오 2: 스케줄러 실행 (23:15, 장 마감 후)
```
스케줄러: _run_parallel_analysis()
→ get_analysis_date() → 어제 날짜 반환 (장 마감 후)
→ generate_technical_recommendations(end_date=어제)
  → 최근 6개월 데이터로 지표 계산
  → 어제 날짜로 저장
→ fetch_and_store_sentiment_for_recommendations(start_date=어제, end_date=어제)
  → 어제 날짜 기준으로 뉴스 수집
  → 어제 날짜로 저장
```

### 시나리오 3: 스케줄러 실행 (06:05, 장 개장 전)
```
스케줄러: _run_economic_data_update()
→ get_analysis_date() → 어제 날짜 반환 (장 개장 전)
→ update_economic_data_in_background(start_date=어제, end_date=어제)
  → 어제 날짜 데이터 수집
  → 어제 날짜로 저장
```

## ✅ 최종 목표

1. **API**: 항상 현재 날짜 기준으로 동작 (사용자 명시적 날짜 지정 가능)
2. **스케줄러**: 실행 시점의 기준 날짜로 동작 (장 마감 여부에 따라 어제/오늘 결정)
3. **데이터 일관성**: 수집 날짜와 분석 날짜가 일치
4. **명확한 로직**: 날짜 처리 로직이 명확하고 예측 가능

