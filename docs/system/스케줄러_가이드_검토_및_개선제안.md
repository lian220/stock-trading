# 📊 스케줄러 가이드 검토 및 개선 제안

## 🔍 검토 결과 요약

### ✅ 잘 설계된 부분

1. **프로세스 순서의 논리성**
   - 23:00 Vertex AI 예측 → 23:45 분석 → 00:00 매수 순서가 논리적
   - 장 시작 전에 모든 분석이 완료되어 매수 결정에 활용 가능

2. **장 시간 체크**
   - 뉴욕 시간 기준으로 정확하게 체크
   - 주말 체크 로직 포함
   - 서머타임 자동 고려

3. **데이터 수집 타이밍**
   - 장 마감 후(06:05) 데이터 수집이 적절
   - 다음 거래일을 위한 데이터 준비 완료

### ⚠️ 발견된 문제점

#### 1. 가이드 문서와 실제 코드 불일치

**문제:**
- 가이드에는 3개 스케줄러만 설명되어 있음 (매수, 매도, 경제 데이터)
- 실제 코드에는 **5개 작업**이 스케줄링됨:
  1. 경제 데이터 (06:05)
  2. **Vertex AI 예측 (23:00)** ← 가이드에 없음
  3. **분석 작업 (23:45)** ← 가이드에 없음
  4. 매수 (00:00)
  5. 매도 (1분마다)

**영향:**
- 사용자가 전체 프로세스를 이해하기 어려움
- 문서 신뢰도 저하

#### 2. 매도 스케줄러 실행 주기

**현재:** 1분마다 실행

**문제점:**
- 너무 빈번한 실행으로 API 호출 부하 증가
- 실제로는 1분마다 매도 조건이 크게 변하지 않음
- 비용 효율성 저하

**권장:** 5분마다 실행 (또는 3분)

#### 3. 매수 스케줄러 실행 시간

**현재:** 매일 00:00 (한국 시간)

**문제점:**
- 00:00은 미국 장 시간(9:30-16:00 ET)과 겹치지 않음
- 한국 시간 00:00 = 뉴욕 시간 전날 10:00 (또는 11:00 서머타임)
- 실제로는 장 시작 직전(23:30) 또는 장 시작 직후(00:30)가 더 적절할 수 있음

**권장:** 
- 장 시작 직전: 23:30 (뉴욕 09:30 직전)
- 또는 장 시작 직후: 00:30 (뉴욕 09:30 직후)

#### 4. 매도 조건의 우선순위

**현재 로직:**
```python
# 조건 3: 기술적 지표 중 3개 이상 매도 신호 (가장 강력한 매도 신호부터 체크)
if technical_sell_signals >= 3:
    sell_reasons.append(...)
# 조건 2: 감성 점수 < -0.15이고 기술적 지표 중 2개 이상 매도 신호
elif sentiment_score < -0.15 and technical_sell_signals >= 2:
    sell_reasons.append(...)
```

**문제점:**
- 익절/손절 조건이 기술적 지표보다 우선순위가 낮음
- 실제로는 **익절/손절이 가장 우선**되어야 함 (리스크 관리)

**권장:** 익절/손절 조건을 가장 먼저 체크하고, 조건 충족 시 즉시 매도

#### 5. 중복 매수 방지 로직 주석 처리

**코드 838-840줄:**
```python
# 이미 보유 중인 종목인지 확인
# if pure_ticker in holding_tickers:
#     logger.info(f"[{function_name}] {stock_name}({ticker}) - 이미 보유 중인 종목이므로 매수하지 않습니다.")
#     continue
```

**문제점:**
- 중복 매수 방지 로직이 주석 처리되어 있음
- 같은 종목을 여러 번 매수할 수 있음 (포트폴리오 집중도 증가)

**권장:** 중복 매수 방지 로직 활성화 또는 조건부 활성화 옵션 추가

---

## 💡 주식 전문가 관점 개선 제안

### 1. 프로세스 최적화 제안

#### 현재 프로세스
```
06:05 - 경제 데이터 수집
23:00 - Vertex AI 예측
23:45 - 기술적 지표 + 감정 분석
00:00 - 매수 실행
(1분마다) - 매도 체크
```

#### 개선된 프로세스 제안

**옵션 A: 장 시작 전 완료형 (현재 구조 유지)**
```
06:05 - 경제 데이터 수집 (장 마감 후)
23:00 - Vertex AI 예측 (장 시작 전)
23:45 - 기술적 지표 + 감정 분석 (장 시작 전)
23:50 - 매수 대상 최종 검토 및 우선순위 확정
00:00 - 매수 실행 (장 시작 직전)
(5분마다) - 매도 체크 (장 시간 중)
```

**옵션 B: 장 시작 후 실행형 (더 신뢰도 높음)**
```
06:05 - 경제 데이터 수집
23:00 - Vertex AI 예측
23:45 - 기술적 지표 + 감정 분석
00:30 - 매수 실행 (장 시작 후 30분, 시장 안정화 후)
(5분마다) - 매도 체크
```

**추천:** 옵션 A (현재 구조 유지하되 시간 조정)

### 2. 매도 스케줄러 개선

#### 현재
- 1분마다 실행
- 모든 보유 종목 체크

#### 개선안
```python
# 장 시간대별 다른 주기
- 장 시작 직후 (09:30-10:00 ET): 1분마다 (변동성 큰 시간)
- 정규 장 시간 (10:00-15:30 ET): 5분마다
- 장 마감 직전 (15:30-16:00 ET): 1분마다 (익절/손절 집행)
```

### 3. 리스크 관리 강화

#### 현재 손절/익절 기준
- 익절: +5%
- 손절: -7%

#### 개선 제안

**A. 트레일링 스톱 (Trailing Stop) 추가**
```python
# 최고가 대비 일정 % 하락 시 매도
if current_price >= highest_price * 1.05:  # 5% 이상 수익
    if current_price <= highest_price * 0.98:  # 최고가 대비 2% 하락
        sell()  # 트레일링 스톱 매도
```

**B. 시간 기반 손절**
```python
# 보유 기간이 길수록 손절 기준 완화
holding_days = (current_date - purchase_date).days
if holding_days > 30:  # 30일 이상 보유
    stop_loss = -10%  # 손절 기준 완화
elif holding_days > 60:
    stop_loss = -12%
```

**C. 시장 상황별 동적 조정**
```python
# VIX 지수 기반 손절 기준 조정
if vix > 30:  # 변동성 큰 시장
    stop_loss = -10%  # 손절 기준 완화
elif vix < 15:  # 안정적인 시장
    stop_loss = -5%  # 손절 기준 강화
```

### 4. 매수 금액 분산 전략

#### 현재
- 1주씩 매수
- 잔고가 있으면 계속 매수

#### 개선안

**A. 포트폴리오 비중 제한**
```python
# 종목당 최대 비중 제한 (예: 20%)
max_position_size = total_portfolio * 0.20
if current_position_value + purchase_amount > max_position_size:
    skip()  # 비중 초과 시 매수 건너뜀
```

**B. 계층적 매수 (Pyramiding)**
```python
# 첫 매수: 1주
# 조건 충족 시 추가 매수: +0.5주
# 최대 3회까지 추가 매수 가능
```

**C. 시장 상황별 매수 금액 조정**
```python
# VIX가 높으면 (변동성 큰 시장) 매수 금액 감소
if vix > 25:
    purchase_amount = base_amount * 0.5  # 절반만 매수
elif vix < 15:
    purchase_amount = base_amount * 1.5  # 1.5배 매수
```

### 5. 시장 상황 모니터링 추가

#### 제안 기능

**A. 시장 전반 모니터링**
```python
# 장 시작 전 시장 지수 체크
sp500_change = get_sp500_premarket_change()
if sp500_change < -2%:  # 프리마켓 2% 이상 하락
    skip_buy()  # 매수 건너뜀
    send_alert("시장 하락세, 매수 보류")
```

**B. 섹터별 모니터링**
```python
# 매수 대상 종목의 섹터 전체 상황 체크
sector_performance = get_sector_performance(ticker)
if sector_performance < -3%:  # 섹터 3% 이상 하락
    reduce_position_size()  # 매수 금액 감소
```

**C. 뉴스 이벤트 체크**
```python
# 중요 뉴스 이벤트 전후 매수 보류
if has_important_news_event(ticker, hours=2):
    skip_buy()  # 2시간 전후 매수 보류
```

### 6. 백테스팅 및 성과 추적

#### 제안 기능

**A. 매수/매도 로그 저장**
```python
# 모든 매수/매도 거래를 DB에 저장
- 매수 시점, 가격, 이유
- 매도 시점, 가격, 이유, 수익률
- 보유 기간
```

**B. 성과 분석 대시보드**
```python
# 주간/월간 성과 리포트
- 총 수익률
- 승률 (Win Rate)
- 평균 보유 기간
- 종목별 수익률
- 매수/매도 이유별 성과
```

**C. 전략 최적화**
```python
# 과거 데이터로 최적 손절/익절 비율 찾기
# 최적 매수 조건 조합 찾기
# 시장 상황별 최적 전략 찾기
```

---

## 📋 즉시 적용 가능한 개선 사항

### 우선순위 1 (즉시 적용)

1. **가이드 문서 업데이트**
   - Vertex AI 예측 (23:00) 추가
   - 분석 작업 (23:45) 추가
   - 전체 프로세스 흐름도 추가

2. **매도 스케줄러 주기 조정**
   ```python
   # 1분 → 5분으로 변경
   schedule.every(5).minutes.do(self._run_auto_sell)
   ```

3. **중복 매수 방지 로직 활성화**
   ```python
   # 주석 해제
   if pure_ticker in holding_tickers:
       logger.info(f"{stock_name}({ticker}) - 이미 보유 중인 종목이므로 매수하지 않습니다.")
       continue
   ```

### 우선순위 2 (단기 개선)

4. **익절/손절 우선순위 조정**
   ```python
   # 매도 조건 체크 순서 변경
   # 1순위: 익절/손절
   # 2순위: 기술적 지표
   # 3순위: 감정 분석
   ```

5. **매수 시간 조정**
   ```python
   # 00:00 → 23:50으로 변경 (장 시작 직전)
   schedule.every().day.at("23:50").do(self._run_auto_buy)
   ```

6. **포트폴리오 비중 제한 추가**
   ```python
   # 종목당 최대 비중 20% 제한
   max_position_value = total_portfolio * 0.20
   ```

### 우선순위 3 (중기 개선)

7. **트레일링 스톱 구현**
8. **시장 상황 모니터링 추가**
9. **성과 추적 시스템 구축**

---

## 🎯 최종 권장 프로세스

### 일일 프로세스 (한국 시간 기준)

```
06:05 - 경제 데이터 수집
        ↓
23:00 - Vertex AI 주가 예측
        ↓
23:45 - 기술적 지표 + 감정 분석
        ↓
23:50 - 매수 대상 최종 검토
        ↓
00:00 - 자동 매수 실행 (장 시작 직전)
        ↓
(5분마다) - 자동 매도 체크 (장 시간 중)
```

### 주간 프로세스

- **월요일**: 주간 성과 리포트 생성
- **금요일**: 주간 전략 리뷰 및 조정

### 월간 프로세스

- **월초**: 전월 성과 분석
- **월말**: 전략 최적화 및 파라미터 조정

---

## 📊 예상 효과

### 개선 전 vs 개선 후

| 항목 | 개선 전 | 개선 후 | 효과 |
|-----|--------|--------|------|
| 매도 체크 주기 | 1분 | 5분 | API 호출 80% 감소 |
| 중복 매수 | 가능 | 방지 | 포트폴리오 집중도 감소 |
| 매수 타이밍 | 00:00 | 23:50 | 장 시작 직전 실행 |
| 리스크 관리 | 기본 | 강화 | 손실 최소화 |
| 문서 정확도 | 60% | 100% | 사용자 이해도 향상 |

---

## 🔧 구현 가이드

각 개선 사항의 구체적인 구현 방법은 별도 문서로 작성 예정입니다.

**관련 문서:**
- [스케줄러 가이드](./스케줄러_가이드.md) - 현재 가이드
- [매수 동작 가이드](./매수_동작_가이드.md) - 매수 시스템 상세
- [추천 시스템 가이드](../recommendation/통합추천시스템_가이드.md) - 추천 로직 상세

---

**작성일:** 2025-01-XX  
**검토자:** AI 주식 전문가  
**상태:** 검토 완료, 개선 제안 완료




